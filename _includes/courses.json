{
  "@context": "http://schema.org",
  "@type": "Course",
  "name": "{{ page.title }}",
  "description": "{% if page.meta %}{{ page.meta }}{% else %}{{site.description}}{% endif %}",
  "provider": {
    "@type": "Organization",
    "name": "{{ site.title }}",
    "sameAs": "{{ site.url }}"
  }
}
